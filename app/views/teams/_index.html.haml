=# link_to "back to dashboard", dashboard_path
#closeAllTeamsView Close

%h1
  Teams list

- @teams.each do |team|
  .team
    %div
      Team:
      = team.title
    %div
      Age Group:
      = team.age_group
    %div
      Number of Players:
      = team.num_of_players
    %div
      create at:
      = team.created_at
    %div
      = link_to "show", team_path(team.id)
      |
      = link_to "edit", "#"
      |
      = link_to "delete", "#"

